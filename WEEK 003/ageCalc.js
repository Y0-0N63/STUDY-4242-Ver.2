function ageCalc() {
  // 변수 result에 21 값 담기
  var result = 21;

  // 변수의 값(21)과 타입(정수)이 같으면
  if (result === 21) {
    var str = 'Correct, My age is ';
    alert(str + result);
  } else {
    alert('Try Again');
  }
}
